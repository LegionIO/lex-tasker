RSpec.describe Legion::Extensions::Tasker do
  it "has a version number" do
    expect(Legion::Extensions::Tasker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
